// Unit tests for content-build

mod process_content_tests;
mod find_content_tests;
mod build_content_tests;
mod generate_output_tests;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert!(true);
    }
}