[package]
name = "common_traits"
version = "0.1.0"
edition = "2021"
description = "Common trait implementations for the writing toolkit"
authors = ["Evan Sims <hello@evansims.com>"]

[dependencies]
# Internal dependencies
common-errors = { path = "../errors" }

# External dependencies
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
tempfile = "3.3"
